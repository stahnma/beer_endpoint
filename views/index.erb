<html>
  <head>
    <title>The Beer at the Puppet HQ</title>
    <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon"
    <meta http-equiv="imagetoolbar" content="no">
    <meta http-equiv="content-language" content="EN">
    <meta charset="utf-8" />
    <meta http-equiv="refresh" content="300">
    <style type="text/css">
    body {
      margin: 0;
      padding: 0;
      background:rgba(240,240,240,1);
    }

    h1,h2,h3,h4,section,header,footer,div {
      font-family: "Helvetica Neue",Helvetica,sans-serif;
      /*border:solid 1px red;*/
    }

    a,a:link {
      color: black;
      font-weight: 400;
    }

    header {
      max-height:20%;
    }

    header img{
      height:1em;
      margin-right:0.1em;
    }

    p {
      margin: 0em 0em 0.1em 0em;
      min-height: 1em;
      max-width: 80%;
    }

    h1 {
      font-size: 1.9em;
      line-height: 100%;
      letter-spacing: -0.9;
      margin-left:0.5em;
      padding: 0.5em 0em 0em 0em;
    }

    h2 {
      font-size: 2em;
      line-height:105%;
      height:100%;
      margin: 0.3em 0 0 0;
      /*min-height:20%;*/
      max-height:30%;
    }

    h3 {
      font-size: 1.6em;
      line-height: 100%;
      margin: 1.0em 0 0 0;
      color: rgba(00,00,00,0.6);
    }

    h4{
      margin: 0;
      font-size:0.9em;
      font-weight:500;
    }

    #tap-table {
      margin-left:0.8em;
      width:25%;
      height:100%;
      background:#fff;
      display: -webkit-flex;
      display:         flex;
      -webkit-flex-direction:column;
              flex-direction:column;
    }

    #tap-table section {
      padding:0.8em;
    }

    .vertical-container {
      height:80%;
      display: -webkit-flex;
      display:         flex;
      -webkit-align-items: center;
              align-items: center;
      -webkit-justify-content: center;
              justify-content: center;
    }

    .label {
      /*color: rgba(255,174,26,1);*/
      opacity: 0.5;
    }

    .value {
      font-weight: 600;
    }

    footer {
      font-size:10px;
      text-align:center;
      padding:1em;
    }

    .beer-data {
      font-size:0.9em;
      margin:0 0 0 0;
    }

    .beer-data a {
      color: rgba(255,174,26,1);
    }

    </style>
  </head>

  <body>

    <header>
      <h1><img src="puppet_mark.png" alt="Puppet" />puppet office kegs</h1>
    </header>

    <div class="vertical-container">
      <% count = 0 %>
      <% @kegerator.each do |keg| %>
      <% count = count +1 %>

      <div id="tap-table">
        <style>
          div :last-child {margin-right:1em;}
        </style>
        <section>
          <h4><%= keg[:tap].upcase %></h4>
          <h3><%= keg[:brewery] %></h3>
          <h2><%= keg[:beer_name] %></h2>
          <div class="beer-data">
            <p><span class="label">Style:</span> <span class="value"><%= keg[:style] %></span></p>
            <p><span class="label">ABV:</span> <span class="value"><%= keg[:abv] %>%</p>
            <p><span class="label">IBU:</span> <span class="value"><%= keg[:ibu] %></span></p>
            <p><span class="label">Tap Date:</span> <span class="value"><%= keg[:tap_date] %></span></p>
            <p><span class="label">Beer Advocate:</span> <span class="value"><a href='<%= keg[:link] %>'>Link</a></p>
          </div>
        </section>
      </div>

      <% end %>
    </div>

    <footer>
      &copy; <%= Time.now.year %> Kerminator Frogbot and <a href='http://twitter.com/stahnma'>@stahnma</a> | Find the source on <a href='http://github.com/stahnma/beer_endpoint'>github.</a>
    </footer>
  </body>
</html>
